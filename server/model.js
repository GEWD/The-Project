//Use for any functionality that deals with data: database query, API query, etc.

exports.tripNameInsert = (tripName) => {
  //Insert tripname into the database 
  console.log(`Successfuly added ${tripName} to the database`);
};